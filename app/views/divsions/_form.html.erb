<%= form_for(@divsion) do |f| %>
  <% if @divsion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@divsion.errors.count, "error") %> prohibited this divsion from being saved:</h2>

      <ul>
      <% @divsion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
